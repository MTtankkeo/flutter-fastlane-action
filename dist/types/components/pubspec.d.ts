/** Utility class for reading and parsing a Dart/Flutter pubspec.yaml file. */
export declare class Pubspec {
    /** Reads a YAML file at the given path and parses its content. */
    static parse(path: string): any;
}
